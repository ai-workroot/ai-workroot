# Development Scripts

This directory contains developer-only helpers, release validation wrappers,
review export tools, and smoke helpers.

These scripts may call package code under `src/ai_workroot/`, but they are not
the product implementation surface.

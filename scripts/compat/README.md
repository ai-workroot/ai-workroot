# Compatibility Wrappers

This directory contains short compatibility wrappers for historical script
entry points.

Wrappers must delegate to `src/ai_workroot/` package modules. Do not add new
Clean Workroot product logic here.

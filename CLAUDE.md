@AGENTS.md

## Claude Code Adapter

Use `AGENTS.md` as the shared source of project rules.

Claude-specific memory or plugin behavior must not replace the Workroot. Durable context belongs in the Workroot files.

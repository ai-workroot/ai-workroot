# Export And Import Interface

Export and import preserve portability.

They must define export scope, import scope, file format, path mapping, conflict handling, sensitive-data handling, generated-store exclusion, and validation after import.

Exports must not require a specific AI agent, model provider, database, operating system, or hosted service.

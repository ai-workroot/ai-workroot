# Workroot Operating Protocol For Codex

Use `AGENTS.md` and `docs/workroot-operating-protocol.md` as the source of truth.

This adapter exists so Codex can follow the Workroot protocol without duplicating rules.

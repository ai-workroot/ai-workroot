# Capabilities

Capabilities are reusable workflows or role/domain operating procedures.

The core capability is `workroot-operating-protocol`.

Future capabilities should extend the Workroot. They must not replace the core memory, task, index, privacy, or continuity rules.

Before adding a role or domain capability, read `docs/extension-contract.md`.
